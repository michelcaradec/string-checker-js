export enum ConfidenceLevel {
    Unknown,
    Technical,
    Message
}

export enum DictionaryType {
    IncludeToken,
    ExcludeToken,
    ExcludeFile,
    ExcludeFolder
}

export enum TreeViewType {
    FileToTokens,
    TokenToFiles
}
